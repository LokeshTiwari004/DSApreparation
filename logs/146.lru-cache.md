# 146. LRU Cache

## Problem
- **Title:** LRU Cache
- **Number:** #146
- **Difficulty:** Medium
- **Link:** https://leetcode.com/problems/lru-cache/

## User's Approach
Uses `collections.OrderedDict`. `get`: delete + re-insert (moves to newest end). `put`: if exists, delete + update; if at capacity, `popitem(last=False)` evicts oldest; then insert.

## Correctness
Correct. `OrderedDict` preserves insertion order. Re-inserting moves a key to the newest end. `popitem(last=False)` removes the key at the oldest end.

## Pattern Analysis
**Pattern: Design with OrderedDict.**
The interview-expected approach uses a custom doubly-linked list + hash map:
```python
class LRUCache:
    def __init__(self, capacity):
        self.cap = capacity
        self.cache = {}  # key -> node
        self.head = ListNode(0, 0)
        self.tail = ListNode(0, 0)
        self.head.next = self.tail
        self.tail.prev = self.head

    def _remove(self, node):
        node.prev.next = node.next
        node.next.prev = node.prev

    def _add(self, node):
        node.prev = self.tail.prev
        node.next = self.tail
        self.tail.prev.next = node
        self.tail.prev = node

    def get(self, key):
        if key in self.cache:
            node = self.cache[key]
            self._remove(node)
            self._add(node)
            return node.val
        return -1

    def put(self, key, value):
        if key in self.cache:
            self._remove(self.cache[key])
        node = ListNode(key, value)
        self.cache[key] = node
        self._add(node)
        if len(self.cache) > self.cap:
            lru = self.head.next
            self._remove(lru)
            del self.cache[lru.key]
```
`OrderedDict` is the Pythonic shortcut, but the manual DLL approach tests understanding.

## Complexity
- **Time:** O(1) per operation (get, put)
- **Space:** O(capacity) — stores at most capacity key-value pairs

## Code Quality
Clean and concise (13 lines of logic). Uses Python's built-in `OrderedDict` which is the idiomatic solution. No header notes recorded. The `import` inside `__init__` is unusual — standard is at module top.

## Key Takeaways
- LRU Cache = hash map + ordered structure (DLL or OrderedDict)
- `OrderedDict` is the Python trick; DLL + map is the language-agnostic approach
- `popitem(last=False)` removes the oldest entry (LRU)
- Delete + re-insert = mark as recently used
- Know both the Pythonic and the generic implementation for interviews

## Related Problems
- #460 LFU Cache (frequency-based eviction — harder)
- #706 Design HashMap (hash map from scratch)
- #155 Min Stack (another design problem)
